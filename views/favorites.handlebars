<main class="bookworm-main">
    <p>Check out your saved books!</p>
    <span class="search-bar">
        <input class="filter-bar" type="text" placeholder="Filter by typing an author or work">
    </span>
    <ul class="search-results">
        {{#each books}}
        <li id="wid-{{workid}}" class="li-cont">
            <span id="work-inf">
                <p>Title: {{titleweb}}</p>
                <p>Author: {{authorweb}}</p>
                <p>ID: {{workid}}</p>
                <p>Comment: {{comment}}</p>
            </span>
            <span class="edit-btngroup fav-btngroup">
                <input id="edit-fave" type="submit" value="edit" onclick="editBook({{workid}})"></input>
                <input id="visible-remove" type="submit" value="remove" onclick="removeFromColl({{workid}})"></input>
            </span>
        </li>
        {{/each}}
    </ul>
</main>
